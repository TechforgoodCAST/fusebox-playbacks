<p id="notice"><%= notice %></p>

<p>
  <strong>Organisation Name:</strong>
  <%= @playback.organisation_name %>
</p>

<%- @playback.insights.each do |insight| %>
  <li><%= insight.name %></li>
  <%- insight.steps.each do |step| %>
    <li><%= step.name %></li>
  <% end %>
<% end %>

<%= link_to 'Edit', edit_playback_path(@playback) %> |
<%= link_to 'Back', playbacks_path %>